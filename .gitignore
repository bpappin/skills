.env
node_modules/
*.zip
.DS_Store
*.skill

# Working notes: handoffs, move briefs, who to talk to and what to say.
# Written against live projects and full of names, instances and addresses
# that must never reach a public history. Local only, the whole directory.
docs/outbox/

# scratch: things retired but not yet deleted
_to_delete/
_retired/
